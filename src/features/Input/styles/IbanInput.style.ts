// React-Native Imports
import {StyleSheet} from 'react-native'

export const IbanInputStyles = () =>
  StyleSheet.create({
    closeIcon: {
      position: 'absolute',
      top: 0,
      right: 10,
      bottom: 0,
    },
  })
