// React-Native Imports
import {StyleSheet} from 'react-native'

export const PasswordInputStyles = () =>
  StyleSheet.create({
    showPasswordIcon: {
      position: 'absolute',
      top: 0,
      right: 10,
      bottom: 0,
    },
  })
