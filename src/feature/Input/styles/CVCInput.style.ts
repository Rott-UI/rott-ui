// React-Native Imports
import {StyleSheet} from 'react-native'

export const CVCInputStyles = () =>
  StyleSheet.create({
    infoIcon: {
      position: 'absolute',
      top: -22,
      left: 80,
    },
  })
