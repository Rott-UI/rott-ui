import {StyleSheet} from 'react-native'

export const AmountInputStyles = () =>
  StyleSheet.create({
    amountInputStyle: {
      width: 'auto',
    },
    amountInputCurrencyLabel: {
      position: 'absolute',
      right: '2.5%',
    },
  })
